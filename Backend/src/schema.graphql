# import * from './generated/prisma.graphql'

type Mutation {
  createUser(
    name: String!
    email: String!
    smallImage: String
    largeImage: String
  ): User!
}

type Query {
  users: [User]!
}
